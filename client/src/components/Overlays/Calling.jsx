import Button from '../Buttons/Button'

export default function Calling(props) {

  return (
    <div>
      <Button 
        call 
        onClick={() => {
        }}
      />
    <br />
    <h2>Calling attendee...</h2>
  </div>
  )
}